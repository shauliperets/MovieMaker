/*import { configureStore } from "@reduxjs/toolkit";
//import { counterSlice } from "../Features/CounterSilice";
import { counterSlice } from "../Components/Counter/counterSlice";

export default configureStore({
  reducer: {
    counter: counterSlice,
  },
});*/
